# ── Elasticsearch Local ──────────────────────────────────────────
# Local installation via Docker (elastic.co/start-local)
ELASTICSEARCH_URL=http://localhost:9200

# Read-only API Key for all indices
# Generated in Kibana DevTools — Step 5 of devtools_fresh_produce_jina.md
# Copy the value of the "encoded" field (not api_key!)
ELASTICSEARCH_API_KEY=YOUR_ENCODED_VALUE_HERE
